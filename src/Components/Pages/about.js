import React from 'react';
// import { useSearchParams } from 'react-router-dom';

const About = () => {

    // const [params, n] = useSearchParams();
    // console.log("id:",params.get("id"));
    // console.log("Keys:",params.keys());

    

    return (
        <div>
            <h1>About page</h1>
        </div>
    );
};

export default About;